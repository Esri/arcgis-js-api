{
  "title": "environment",
  "type": "object",
  "description": "Represents settings that affect the environment in which the web scene is displayed (such as lighting). It is part of the initial state of the web scene as well as slides in the presentation.",
  "properties": {
    "lighting": {
      "type": "object",
      "$ref": "lighting_schema.json"
    }
  },
  "additionalProperties": false
}
